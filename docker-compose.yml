name: nc-network-error-detection

services:
  web:
    build: .
    container_name: nc-network-web
    restart: unless-stopped
    networks:
      - proxy
    expose:
      - "80"

networks:
  proxy:
    external: true
